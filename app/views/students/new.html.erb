<h1>Student Form</h1>
<%= form_tag(@student) do %>
  <%= label_tag 'student',  text_field_tag('student[first_name]') %>
  <%= label_tag 'student',  text_field_tag('student[last_name]') %>
  <%= submit_tag 'Submit Student' %>
<% end %>